#ifndef SENSOR_H
#define SENSOR_H

#include <ncurses.h>  // Include ncurses library for terminal handling

// Function prototype for reading and logging sensors
void read_and_log_sensors(WINDOW *win);

#endif // SENSOR_H
